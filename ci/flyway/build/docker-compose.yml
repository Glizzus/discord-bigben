version: '3.8'

services:
  flyway:
    image: ghcr.io/glizzus/bigben/flyway:build-${DATE}
    build:
      context: ../../../migrations
      args:
        BUILD_TIME: ${DATE}
        GIT_HASH: ${GIT_HASH}
