version: '3.8'

services:
  chimer:
    image: ghcr.io/glizzus/bigben/chimer:build-${DATE}
    build:
      context: ../../../chimer
      args:
        BUILD_TIME: ${DATE}
        GIT_HASH: ${GIT_HASH}
